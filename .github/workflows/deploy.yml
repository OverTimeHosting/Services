name: Deploy to OTHCloud

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Deployment
        run: |
          curl -X POST https://othcloud.xyz/api/deploy/4cOCUkHqaN160YM6B9qBW
